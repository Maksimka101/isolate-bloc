name: integration_tests
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  isolate_bloc:
    path: ../../packages/isolate_bloc/

  cupertino_icons: ^1.0.2
  freezed_annotation: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  build_runner: ^2.1.9
  freezed: ^1.1.1

flutter:
  uses-material-design: true

  assets:
    - assets/test_text_asset.txt
