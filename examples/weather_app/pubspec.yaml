name: weather_app
description: Showcase for isolate_bloc architecture pattern.

version: 1.0.0+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  # isolate_bloc: ^2.0.0
  isolate_bloc:
    path: ../../packages/isolate_bloc/
  equatable: 2.0.3
  http: 0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
